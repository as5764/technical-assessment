# Application
PORT=8080

# AWS — do NOT set these in production; use IRSA (IAM Role for Service Account)
# For local testing only:
AWS_REGION=ap-south-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_PROFILE=

# Kubernetes — not needed in-cluster (ServiceAccount token is auto-mounted)
# For local testing only:
# KUBECONFIG=~/.kube/config
